# omni.yaml
# 全向车平台配置

controller_ros:
  ros__parameters:
    platform:
      type: "omni"

controller:
  system:
    platform: "omni"
  mpc:
    horizon: 20
    dt: 0.1
    v_max: 2.0
    omega_max: 1.5
